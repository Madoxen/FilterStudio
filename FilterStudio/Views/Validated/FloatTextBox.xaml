<UserControl x:Class="FilterStudio.Views.Validated.FloatTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FilterStudio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800" x:Name="Root"
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="InputTextBox" Text="{Binding ElementName=Root,UpdateSourceTrigger=Explicit, Path=Text, Mode=TwoWay}"
                    TextChanged="CheckForInvalidState" Loaded="CheckForInvalidState" GotFocus="CheckForInvalidState" LostFocus="CheckForInvalidState"
                    VerticalContentAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        </TextBox>
    </Grid>
</UserControl>
